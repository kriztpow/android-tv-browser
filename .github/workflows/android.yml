name: Android CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      # 1. Clonar el repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Configurar JDK 11 usando Zulu (más estable)
      - name: Set up JDK 11 (Zulu)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 11

      # 3. Configurar caché de Gradle
      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle

      # 4. Dar permisos de ejecución al gradlew
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 5. Descargar dependencias
      - name: Download dependencies
        run: ./gradlew dependencies --no-daemon

      # 6. Compilar APK en modo debug
      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace --no-daemon

      # 7. Subir el APK como artefacto
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-tv-browser-apk
          path: app/build/outputs/apk/debug/app-debug.apk
