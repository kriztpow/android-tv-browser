name: Android CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      # 1. Clonar el repositorio
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Configurar JDK 17 (requerido para Android Gradle Plugin 8+)
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: 17

      # 3. Configurar caché de Gradle para acelerar compilaciones
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle

      # 4. Dar permisos al gradlew
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 5. Descargar dependencias
      - name: Download dependencies
        run: ./gradlew dependencies

      # 6. Compilar APK en modo debug
      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace

      # 7. Guardar el APK como artefacto descargable
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: android-tv-browser-apk
          path: app/build/outputs/apk/debug/app-debug.apk
